# Orchestration Log: Pete (2026-03-28T09:39:21Z)

## Role
Infrastructure Automation Specialist — CI/CD, Scripts, Deployment Workflows

## Assignment
CI/CD and scripts gap analysis — confirm deploy-azure.yml missing pull secret creation step and ghcrImagePullRef param, verify no local build script exists

## Outcome

### CI Workflow Analysis (deploy-azure.yml)

**Gaps Identified:**
1. **Missing imagePullSecret creation** (after line 196, before line 197)
   - No step creates Kubernetes `docker-registry` secret in CI workflow
   - Provided implementation: kubectl create secret with `--dry-run=client | kubectl apply`

2. **Missing parameter in rad deploy** (line 200-203)
   - Current: `./rad deploy infra/radius/app.bicep --parameters containerRegistry="$GHCR_PREFIX" --parameters imageTag="$IMAGE_TAG" --parameters deploymentTarget='radius'`
   - Required: Add `--parameters ghcrImagePullRef="ghcr-pull-secret"`

### Local Developer Workflow Analysis

**Verification:**
- ✅ Dockerfiles exist: `src/expense-api/Dockerfile`, `src/workflow-engine/Dockerfile`, `src/notification-svc/Dockerfile`
- ✅ Commit 2f18c7b exists (referenced in bootstrap.sh)
- ✅ CI image build/push works (lines 119-136)
- ❌ No local build/push script exists: `scripts/build-push-images.sh` is missing

**Provided Solution:**
- `scripts/build-push-images.sh` — Build and push service images locally with tag, authenticate to GHCR, provide deployment instructions
- Optional: `scripts/create-ghcr-pull-secret.sh` — Idempotent imagePullSecret creation

### Decision Contribution
Documented exact line references and tested solutions for both CI workflow and local developer gaps; identified that GHCR packages are private by default (supports Daisy's decision to make them public)

## Status
✅ Complete — Analysis written to inbox (pete-image-push-gaps.md), solution implementations provided, CI/CD and local workflow gaps fully documented
