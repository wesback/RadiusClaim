# Orchestration: Graham (Platform Dev)

**Timestamp:** 2026-03-25T16:05:45Z  
**Agent:** Graham (Platform Dev)  
**Activities:** Traced Dapr component projection failure; found Radius recipes provision Azure resources but do not auto-create Kubernetes Dapr Component objects; reported repo-side fixes and operator recovery.

## Summary

Graham identified and documented a critical platform gap: Radius recipes with `resourceProvisioning: 'recipe'` successfully provision Azure backing resources (Storage, Service Bus, Key Vault) but **do NOT automatically project Kubernetes Dapr Component CRDs**. This causes sidecars to fail with `state store statestore is not configured` errors.

**Deliverables:**
- Root cause analysis: Dapr Component Projection Gap (decision filed)
- Secondary issue: State-store recipe missing `allowSharedKeyAccess: true`
- Recovery script: `scripts/deploy-dapr-components.sh`
- Template: `infra/kubernetes/dapr-components.yaml`
- Operator sequence documented in decisions

**Status:** IMPLEMENTED — Workaround deployed; recipe fix required for production use.

## Decisions Filed

1. **Decision: Dapr Component Projection Gap and Recovery Strategy** — documented in decisions.md
2. **Decision: Dapr Component Wiring Gap in Radius Recipe Output** — documented in decisions.md

## Next Actions for Team

- **Eddie (Docs):** Integrate `deploy-dapr-components.sh` into walkthrough Step 9a and validation checklist
- **Daisy (Lead):** Bootstrap.sh design incorporates Phase 6 (Dapr Component Backfill)
- **Karen (Tester):** Validate corrected walkthrough end-to-end once recipe fix lands

## Impact

- **Blocking:** Current state-store recipe is non-functional (requires `allowSharedKeyAccess: true` fix pre-deployment)
- **Operational:** New deployments require explicit `deploy-dapr-components.sh` step post-application deployment
- **Discovery:** Component projection gap is undocumented in Radius — likely affects other projects

