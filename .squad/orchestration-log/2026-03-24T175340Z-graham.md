# Orchestration: Graham — Daisy Follow-up Batch (C2, C3, C7)

**Timestamp:** 2026-03-24T17:53:40Z  
**Agent:** Graham (Platform Dev)  
**Triggered By:** Daisy post-revert findings

## Work Scope

Aligned recipe contracts and CI bootstrap following Daisy's validation of the `Radius.Compute → Applications.Core` revert. Three critical follow-ups closed.

## Changes Executed

### C2: Pub/Sub Recipe Contract → Service Bus Topics

- **File:** `infra/radius/recipes/azure/pubsub.bicep`
- **Change:** Output now emits `pubsub.azure.servicebus.topics` (was unaligned)
  - Pre-creates demo topic: `expense-notifications`
  - Pre-creates subscriber-facing subscription: `notification-svc`
  - Emits `namespaceName` as Service Bus FQDN (Dapr's topics guidance)
- **Validation:** `az bicep build --file infra/radius/recipes/azure/pubsub.bicep` ✓
- **Impact:** Recipe contract now matches ACA reference path; explicit subscription in `infra/radius/app.bicep` keeps app model teachable

### C3: State Store Contract → v2

- **File:** `infra/radius/recipes/azure/state-store.bicep`
- **Change:** Output now emits `state.azure.blobstorage` version `v2` (was v1)
- **Alignment:** Matches `infra/radius/environments/azure.bicep` (already modeling ACA reference on v2)
- **Validation:** `az bicep build --file infra/radius/recipes/azure/state-store.bicep` ✓
- **Impact:** Dapr component contract now consistent across Radius + ACA paths

### C7: CI Bootstrap Workflow — Remove Unused OIDC

- **File:** `.github/workflows/deploy-azure.yml`
- **Change:** Removed unused `id-token: write` permission; documented intent inline
  - Real trust boundary is `kubeconfig` (reach cluster) + `rad credential register azure sp` (Radius reaches Azure)
  - No `azure/login` added because runner doesn't provision Azure resources directly; Radius does via SP
- **Validation:** YAML parse of `.github/workflows/deploy-azure.yml` ✓
- **Impact:** Workflow now reflects actual auth model; no OIDC path suggested that isn't used

## Validation Summary

**Bicep Builds:**
- ✓ `az bicep build --file infra/radius/app.bicep`
- ✓ `az bicep build --file infra/radius/environments/azure-radius.bicep`
- ✓ `az bicep build --file infra/radius/recipes/azure/pubsub.bicep`
- ✓ `az bicep build --file infra/radius/recipes/azure/state-store.bicep`

**Build & Test:**
- ✓ `dotnet build RadiusClaim.slnx --configuration Release`
- ✓ `dotnet test RadiusClaim.slnx --configuration Release --no-build`

**Workflow:**
- ✓ YAML parse of `.github/workflows/deploy-azure.yml`

## Decision Record

**Entered into:** `.squad/decisions/decisions.md`  
**Title:** "Graham — Daisy follow-ups (C2, C3, C7)"

## Open Blockers

- **GHCR Image Pull 403:** `ghcr.io/sovereignapp/radiusclaim/*:phase1` — recipe artifacts require republishing before next live deploy
- **Recipe Artifact Publishing:** Required before live demo can resume

## Notes

- Demo story is now coherent: both Radius + ACA paths prove the same Dapr component contracts
- Service Bus topics + disabled entity management stays honest because recipe pre-creates the topic/subscription pair the app actually uses
- CI boundary is now teachable: kubeconfig to reach cluster, service principal registration so Radius can reach Azure
