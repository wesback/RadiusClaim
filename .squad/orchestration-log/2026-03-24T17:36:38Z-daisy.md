# Orchestration: Daisy — Radius.Compute Review & Rejection

**Date:** 2026-03-24  
**Agent:** Daisy (Lead)  
**Timestamp:** 2026-03-24T17:36:38Z

## Work Summary

Daisy conducted a comprehensive live Radius deployment review and performed a full-depth codebase audit using Opus 4.6. The review confirmed Critical Finding C1 from the earlier audit: `Radius.Compute/containers` and `Radius.Compute/routes` resource types are not available in the deployed Radius environment.

## Decisions Made

1. **REJECT `Radius.Compute/*` namespace** — Revert app services to `Applications.Core/containers` and public ingress to `Applications.Core/gateways` (stock Radius 0.55 catalog).
2. **Shape changes required** — `Radius.Compute/containers` uses `containers` map + `extensions.daprSidecar`; `Applications.Core/containers` uses singular `container` + `extensions[]` array.
3. **Assign Graham to implement revert** — He authored the original migration and understands shape differences.
4. **Karen to validate** — Fresh `rad deploy` required before merge; compile-time success is not sufficient.
5. **Pick up follow-ups immediately after revert:**
   - C2: Pub/sub recipe type mismatch (Graham)
   - C3: State store version mismatch (Graham)
   - C7: CI workflow missing `azure/login` (Graham)

## Outstanding Items

- Graham's revert implementation
- Karen's fresh deployment validation
- Follow-up recipe and CI fixes

## Next Steps

1. Graham: Implement Radius.Compute → Applications.Core revert with shape changes
2. Karen: Validate revert via fresh `rad deploy`
3. Graham: Fix C2, C3, C7 follow-up items
