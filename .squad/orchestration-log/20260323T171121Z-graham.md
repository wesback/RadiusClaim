---
agent: Graham
role: Platform Dev
session: radius-first-redesign
created_at: 2026-03-23T17:11:21Z
---

# Graham Orchestration Log

## Radius-First Redesign Implementation (2026-03-23)

**Mission:** Implement Daisy's Radius-first redesign: split Azure bootstrap from Radius app deployment, create Azure recipes, restructure CI/CD workflow.

**Deliverables:**
- Split `infra/radius/environments/azure.bicep` into bootstrap + recipes
- Created `infra/radius/recipes/azure/{state-store,pubsub,secrets}.bicep`
- Created `infra/radius/environments/azure-radius.bicep` (Radius environment definition)
- Restructured `.github/workflows/deploy-azure.yml` with separate bootstrap and Radius paths
- Updated `README.md` to reflect Radius-first narrative

**Outcome:** All deliverables completed to specification. Radius (`rad deploy`) is now the primary deployment command. Azure Container Apps fallback is explicit and honestly demoted. Dapr component names remain stable.

**Technical Notes:**
- Correctly identified that Radius targets Kubernetes, not ACA directly.
- Azure bootstrap remains minimal (ACR, ACA env, identity, Log Analytics).
- Recipes provision Storage Account, Service Bus, Key Vault with RBAC for managed identity.
- No app code changes required.

**Karen's Approval:** All acceptance criteria met. Structural verification complete.
