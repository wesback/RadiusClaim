# Daisy Orchestration Log — 2026-03-25

## Summary

Daisy diagnosed and triaged expense submission failures as a Dapr/statestore readiness issue masquerading as a submission contract failure. Confirmed root cause and aligned API + UI messaging to surface truthful 503 dependency failures. Routed any remaining startup wiring work to Graham.

## Work Completed

1. **Root cause analysis**
   - Reproduced submission failure environment
   - Traced failure to first Dapr state call (before workflow startup)
   - Confirmed connection refusal when Dapr sidecar unavailable
   - Ruled out contract/validation issues as primary blocker

2. **Decision: truthful dependency messaging** (2026-03-25)
   - `src/expense-api/Program.cs`: Return `503` RFC 7807 problem details when Dapr state unreachable
   - `src/expense-api/wwwroot/app/app.js`: Parse problem details and surface dependency issue instead of generic failure
   - Preserves `/app` shell accessibility while making runtime dependencies explicit

3. **Clarified dependency scope**
   - Expense listing + submission: require `expense-api` + `statestore`
   - Workflow telemetry: additional dependency on `workflow-engine` service invocation
   - Each dependency now independently reportable, not collapsed into generic "not ready" message

## Integration with Warren

Coordinated with Warren's standardization of submission error payloads to ensure both:
- Backend returns truthful problem details with dependency context
- Frontend parses and surfaces those details correctly

## Routing

Directed any remaining startup/readiness gaps (e.g., Dapr-aware readiness guidance, local runbook tightening) to **Graham** (Platform Dev) for follow-up if needed.

## Owner

Daisy (Lead)

---

**Timestamp:** 2026-03-25T14:28:31Z  
**Status:** COMPLETE
