# Orchestration Log: Graham (2026-03-28T09:39:21Z)

## Role
Platform Dev — Infrastructure, Radius Configuration, Bicep Wiring

## Assignment
Radius Bicep wiring analysis — confirm imagePullSecrets flow is correct in app.bicep and container-service.bicep, identify operational vs. design gaps

## Outcome

### Bicep Wiring Analysis (COMPLETE & CORRECT)
1. **app.bicep (lines 27-28, 88)**
   - ✅ Declares `param ghcrImagePullRef string = ''`
   - ✅ Constructs `pullSecrets` array: `var pullSecrets = empty(ghcrImagePullRef) ? [] : [{ name: ghcrImagePullRef }]`
   - ✅ Passes `imagePullSecrets: pullSecrets` to all three container modules (lines 174, 205, 233)

2. **container-service.bicep (lines 36-40, 67-73)**
   - ✅ Accepts `param imagePullSecrets array = []`
   - ✅ Constructs `podSpec` with `imagePullSecrets` when non-empty
   - ✅ Merges `podSpec` into `runtimes.kubernetes.pod`

### Gap Identification
- **Design:** ✅ No gaps — wiring is complete and correct
- **Operations:** ❌ Gap confirmed — `bootstrap.sh` creates pull secret but CI workflow does not
  - `bootstrap.sh` lines 1407-1412: Creates Kubernetes secret ✅
  - `bootstrap.sh` line 1431: Passes `--parameters "ghcrImagePullRef=ghcr-pull-secret"` ✅
  - `deploy-azure.yml` line 203: **Missing parameter in rad deploy** ❌
  - Local environment: **No build/push script exists** ❌

### Decision Contribution
Validated that infrastructure plumbing is solid; confirmed that the issue is operational (CI workflow missing pull secret step), not design (Bicep wiring)

## Status
✅ Complete — Analysis written to inbox (graham-imagepullsecret-gap.md), design validated, operational gaps documented
